# Fuzz corpus directory for FuzzValidateDomain
# Seed corpus is embedded in the test via f.Add() calls
# Run with: go test -fuzz=FuzzValidateDomain -fuzztime=30s ./internal/domain/
#
# Extended fuzz verification (2026-05-05):
# - 10 minutes runtime, 62M+ executions, 0 crashes
# - Found 881 interesting cases total (878 seed + 3 discovered)
# - Corpus size: 881 entries (all coverage paths explored)
