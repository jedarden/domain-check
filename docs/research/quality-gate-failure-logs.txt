domain-check-build quality-gate failure logs
Captured: 2026-07-04T01:04:40Z
Workflow: domain-check-build-debug-qg-x8bx6

=== EXIT CODE: 2 ===
=== FAILED STEP: build-quality-gate ===

--- stdout ---
+ apk --no-cache add git ca-certificates
+ BRANCH=main
+ git clone --branch main https://x-access-token:***@github.com/jedarden/domain-check.git /workspace
Cloning into '/workspace'...
+ cd /workspace
+ go version
go version go1.26.4 linux/amd64
Running quality gate on branch: main
+ echo 'Running quality gate on branch: main'
+ go vet ./...
  (passes — downloads deps, no vet errors)

+ go test -race ./...
go: -race requires cgo; enable cgo by setting CGO_ENABLED=1

Error: exit status 2
failed to create fsnotify watcher: too many open files

--- ANALYSIS ---
1. PRIMARY FAILURE: go test -race requires CGO_ENABLED=1
   The quality-gate step runs `go test -race ./...` but the Alpine-based
   container (golang:1.26-alpine) may have CGO disabled by default, or the
   environment lacks CGO prerequisites (gcc, musl-dev).

   FIX: Either:
   a) Set CGO_ENABLED=1 in the quality-gate step, OR
   b) Add `apk add gcc musl-dev` before the test step, OR
   c) Remove the -race flag from the test command

2. SECONDARY WARNING: "failed to create fsnotify watcher: too many open files"
   This is a non-fatal warning from fsnotify (likely from the Argo sidecar)
   but worth noting — the container's ulimit for open files may need raising.

3. go vet ./... passes cleanly — no static analysis issues.
