# Crash Verification Report: bf-173o7e

**Date:** 2026-09-02
**Alert bead:** bf-4ifshb (`ALERT: Agent crash on bead bf-173o7e`)
**Verification bead:** domchk-858fecb1 (step 4; children domchk-673bbad9, domchk-dbd69f80, domchk-dd193e0b)
**Agent:** claude-code-glm-4.7
**Reported exit code:** -1 (needle sentinel — not a signal number)
**Status:** ✅ FALSE POSITIVE — work completed, repo healthy, bead already closed; alert is stale-regeneration noise

## Summary

The alert against bead bf-173o7e is a false positive in the operational sense: the git gc work the bead prescribed was completed (repo fully packed by storm end), bf-173o7e itself was closed on 2026-08-17, the repository is verified healthy today, and no remediation is needed. The kill that generated the alert was real but is fully explained: it was one attempt of bf-173o7e's own Aug-14 memcg-OOM retry storm, already root-caused by domchk-17ca8b7d (commit 46f0360).

One precision point must be stated plainly, because it refines two earlier docs:

> **The gc did NOT complete before this particular kill.** The assigned event (attempt #50 of 132) was SIGKILLed by the kernel memory-cgroup OOM killer **76.8 s into** its `git gc --aggressive --prune=now`, mid-operation. Completion was achieved later, by a different storm attempt (one dispatch exited 0 — "orphaned-on-success") plus subsequent scheduled safe-gc runs. The earlier theory in `docs/crash-investigations/bf-173o7e-false-positive.md` and `docs/crash-reports/bf-4x12ec-verification-report.md` ("git subprocess completed in the background / agent killed by a monitor after completion") is **superseded** — the storm doc §5 rules out the timeout-monitor candidate, and the kill mechanism is memcg OOM inside needle's 12 GiB dispatch scope.

## Alert decode (what actually happened)

| Item | Value |
|---|---|
| Assigned alert timestamp | 2026-08-14T14:21:34.162Z — the `HANDLING_RELEASE_DONE` heartbeat (seq 4868), **not** the kill |
| Actual kill | `agent.completed` 2026-08-14T14:21:15.779Z, `exit_code -1`, duration **76.8 s** (dispatched 14:19:58.737Z) |
| Storm position | Attempt **#50 of 132** dispatches of the same duplicate gc bead, 12:58:58Z–23:25:35Z |
| Death mechanism | Kernel **memcg OOM SIGKILL** inside needle's transient `run-*.scope` (MemoryMax=12 GiB, oom_score_adj=200) over 17.20 GiB of loose objects |
| `-1` meaning | Needle's sentinel `ExitStatus::code().unwrap_or(-1)` = "died on a signal needle did not send"; the real signal was SIGKILL |
| Target bead state | bf-173o7e closed 2026-08-17; maintenance-only duplicate of bf-4x12ec (split-child of the bf-5jhvpk chain) |
| Alert regeneration | Children domchk-673bbad9, domchk-dbd69f80, domchk-dd193e0b created 2026-08-26 — 12 days post-crash, 9 days post-closure (stale-alert-regeneration defect; mitigated by closed-bead filtering / dedup / cooldown in `scripts/crash-alert-manager.sh`) |

Kernel logs for Aug-14 are rotated away (current boot begins Aug-15 19:26 EDT), so the mechanism is established from the primary needle log `~/.needle/logs/claude-code-glm-4.7-lab-domain-check-2026-08-14.jsonl` plus the kernel-verified same-period pattern (257 `task=git` `CONSTRAINT_MEMCG` kills on Aug-16, anon-rss hugging the 12 GiB cap). Full derivation: `docs/crash-investigations/bf-173o7e-aug14-storm-root-cause-2026-09-02.md`.

## Did git gc complete successfully?

**For the killed attempt: no.** Attempt #50 died 76.8 s in, before the gc finished — there is no "completed before the crash" for this event, and the transcript proof for sibling attempts shows gc actively running seconds before SIGKILLs (commit 9d374d1).

**For the task as a whole: yes.** Completion came from a later storm attempt (exit 0, orphaned-on-success — the agent finished successfully but the result went unrecognized), and the repo was fully packed by storm end. Subsequent scheduled safe-gc runs have kept it packed since.

## Repository state — verified fresh 2026-09-02 (this bead)

```
$ git count-objects -vH
count: 98            size: 792.00 KiB      # post-commit residue from other agents' same-day commits
in-pack: 10478       packs: 1              # complete pack set: .pack + .idx + .bitmap + .rev
size-pack: 90.18 MiB prune-packable: 0     # vs 17.20 GiB loose / 9.6 MiB pack at crash time
garbage: 0
$ du -sh .git
93M                                           # healthy (<500MB threshold)
$ git fsck --no-progress --no-dangling
(exit 0, zero output)
```

- Single pack `pack-98054595755f56a27981ce0fa7ff3860f4b5ae3e` (Sep 2 11:09 EDT), registered in `objects/info/packs` — a complete set, not a partial/interrupted write.
- All loose objects postdate the last pack write (normal commit activity), so nothing was left unpacked by a dead gc.
- No incomplete operations: no `gc.pid`, no `*.lock`/`*.keep`/`*.tmp.*` under `.git/objects`, no merge/rebase/bisect state; `safe-gc-checkpoint.json` = `complete`; no `gc.log` (git writes it only on failure).
- Child beads: domchk-dbd69f80 ran `git fsck --full` (exit 0, zero findings) and domchk-dd193e0b confirmed pack/count/lock state — both closed.

## Disposition

**FALSE POSITIVE — no action required.**

- The crash was real but fully explained (memcg OOM, attempt #50 of the storm), and the crashing process was the agent scope running the git command embedded in the bead text — **zero domain-check code involvement**.
- The work objective was achieved: repository packed, healthy, `git fsck` clean.
- The target bead was already closed; the alert chain is stale-regeneration noise, not an unresolved failure.
- Root cause and mitigations are already shipped and holding: `scripts/safe-git-gc.sh` (bare `git gc --aggressive` banned), `scripts/crash-circuit-breaker.sh`, `scripts/crash-alert-manager.sh` closed-bead filtering + dedup + cooldown. Zero crashes since Aug-26.

**No further action needed.** Recommend closing alert bead bf-4ifshb with this report as the record.
