All three children are created with the `split-child` label, chained sequentially, and the parent has the `umbrella` label and depends on the last child.

```
SPLIT_COMPLETE: Created 3 children, parent converted to umbrella
```

```
Children: bf-3ih4, bf-1rdq, bf-3s2m
```

Chain: `bf-5tec` → depends on `bf-3s2m` → depends on `bf-1rdq` → depends on `bf-3ih4`
