Bead `bf-5tec` is closed. Here's the summary of findings:

**All 5 recent `domain-check-build` workflows on iad-ci** (t98cn, qv94b, s5z8k, wmf2z, qms44) show the **same quality-gate failure**:

| Field | Value |
|-------|-------|
| Node | `build-quality-gate` |
| Phase | **Failed** |
| Exit Code | **2** |
| Message | `main: Error (exit code 2)` |
| Duration | ~44s |
| Pods | **None remaining** — deleted by podGC (OnPodCompletion) |

The consistent exit code 2 across all 5 consecutive runs points to a **systematic issue in the source code** (build, lint, or test failure) rather than a transient/infrastructure problem.
