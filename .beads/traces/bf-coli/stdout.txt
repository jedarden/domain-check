Bead bf-coli is now closed. Here's the summary:

**Workflow:** `domain-check-build-manual-zwzbk`
**Phase:** Failed
**Node tree:**
- `build-quality-gate` — **Failed** (exit code 127: `git` not found in `golang:1.26-alpine`)
- `resolve-version` — never reached
- `docker-build` — never reached
- `goreleaser-release` — **absent** (confirmed not in workflow spec)

**Root cause:** The `build-quality-gate` step uses `golang:1.26-alpine` but its script runs `git clone` as the first command. Alpine images are minimal and don't include `git` by default, causing exit code 127 (command not found).

**Actions taken:**
- Polled workflow to terminal phase ✅
- Documented results as comment #6 on parent bead bf-4wvi ✅
- Closed bead bf-coli ✅ (via direct DB update — `br close` has a `claimed_at` parsing bug)
- Flushed checkpoint ✅
