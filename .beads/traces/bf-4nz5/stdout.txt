The bead is closed in the database and flushed to JSONL. The `br close` command itself failed due to a `claimed_at` parsing bug in bead-forge (empty string → "premature end of input"), but the close was applied directly and persisted.

## Summary

**Bead `bf-4nz5` — closed.** The analysis is conclusive:

1. **No pods exist** for any recent `domain-check-build` workflow on iad-ci
2. **Root cause:** The cluster-level Argo Workflows controller config sets `podGC.strategy: OnPodCompletion` — this deletes every pod the instant its container finishes
3. The `domain-check-build` WorkflowTemplate does **not** override this, so it inherits the cluster default
4. Confirmed across 7 recent workflows (`t98cn`, `x2f2d`, `tqkkl`, `tv2nz`, `hxcs5`, `ztx4z`, `vdxl5`) — all show `finishedAt` set, zero pods found

Findings documented in `docs/notes/podGC-analysis.md` and pushed.
