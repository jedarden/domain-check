Bead `bf-3bv` is closed. Here's a summary of what was done:

**Committed and pushed:** `docs/notes/09-goreleaser-configuration.md` — comprehensive documentation of `.goreleaser.yml` covering:

- **`project_name`** — sets template variable for naming
- **`before.hooks`** — `go mod tidy` (dependency consistency) and `go generate ./...` (future-proof for code gen)
- **`builds`** — static binaries (`CGO_ENABLED=0`) for 10 OS/arch combinations across linux, darwin, windows, freebsd with stripped ldflags and version injection
- **`archives`** — tar.gz (zip for Windows), human-readable naming, bundles LICENSE + README.md
- **`checksum`** — SHA256 `checksums.txt` for download verification
- **`changelog`** — auto-generated from commits, ascending order, excludes housekeeping prefixes (docs/test/ci/chore/build)
- **`release`** — published immediately, auto-detects prereleases from tag suffix, replaces existing releases for the same tag
