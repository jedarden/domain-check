Done. Here's what I found and fixed:

**Already correct** (from previous commit `c9c1676`):
- CI Pipeline references Argo Workflows (not GitHub Actions YAML)
- API response schemas show the wrapped format (`total`/`succeeded`/`failed`/`duration` + nested `result`)
- `GET /api/v1/check/multi` route documented alongside `?tlds=` on `/api/v1/check`
- Project Structure includes `internal/config/` and `internal/cli/`
- Dockerfile matches the real multi-stage build (non-root user, healthcheck)
- Edge case #45 documents the decision to accept private-suffix domains and reject downstream

**Fixed in this commit** (`aa6dfe8`):
- **CI Pipeline**: Removed incorrect `cargo test` / `cargo-remote` / `rust-verify` references — that's Rust tooling, not Go. Replaced with correct statement that Go tests/lint run locally under cgroup limits.
- **Deployment section**: Fixed `--port 8080` → `--addr :8080` to match the actual config flag.
