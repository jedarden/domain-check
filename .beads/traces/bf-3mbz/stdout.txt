Bead closed. Here's a summary of what was accomplished:

**Task complete.** All acceptance criteria met:

| Criteria | Status |
|----------|--------|
| Debug workflow submitted with podGC override | ✅ Two workflows submitted — `hqwdk` (OnWorkflowCompletion) and `5dxln` (OnWorkflowSuccess) |
| Workflow name recorded | ✅ `domain-check-build-debug-podgc2-5dxln` |
| Quality-gate step completion status captured | ✅ **Failed**, exit code 2 |

**Key findings:**

1. **Root cause of quality-gate failure:** `go test -race ./...` requires `CGO_ENABLED=1`, but `golang:1.26-alpine` disables CGO by default. The fix is to add `CGO_ENABLED=1` to the quality-gate template's `env` list in declarative-config.

2. **podGC strategy for debugging:** Use `OnWorkflowSuccess` — pods are only deleted on success, so they remain available after failure. `OnWorkflowCompletion` still cleans up pods when the workflow fails.
