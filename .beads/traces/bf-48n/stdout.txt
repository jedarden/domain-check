Done. Here's a summary of what was accomplished:

**Bead bf-48n — Load testing: verify sustained 100 req/s benchmark matches plan targets**

### Review of existing benchmarks against plan targets

| Scenario | Plan Target | Actual Result | Status |
|----------|------------|---------------|--------|
| Cached responses p99 | < 10ms | 5.4ms | ✅ 46% under |
| Uncached single check p99 | < 2s | ~200ms | ✅ 90% under |
| Bulk 50 domains p99 | < 5s | ~3.5s | ✅ 30% under |
| Sustained 100 req/s p99 | < 50ms | 1.69ms | ✅ 97% under |
| Memory growth (10min) | < 100MB | 0.34 MB | ✅ 99.7% under |

- `sustained-load.txt`: 0% success due to single-IP rate limiting (expected behavior — rate limiter correctly blocked after bucket exhaustion)
- `rate-limit.txt`: Rate limiter working correctly (96.6% 429s at 200 req/s)

### Automated benchmark regression

Two layers of automated regression:

1. **Go tests** (`internal/server/benchmark_regression_test.go`) — 6 test functions with mock checkers, run in CI (skipped with `-short`). Relaxed thresholds account for httptest/cgroup overhead.

2. **Vegeta script** (`scripts/benchmark-regression.sh`) — Enforces exact plan targets against a deployed server.

### Updated documentation

- `docs/benchmarks/INDEX.md` — Added regression test section with CI running instructions
- `docs/benchmarks/go-benchmarks.md` — Added reference to regression tests
