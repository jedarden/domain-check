Bead `bf-29a0` is complete. Here's the summary:

**Root Cause:** Exit code 2 from `go test -race ./...` in the `domain-check-build` quality-gate step.

**Exact error:** `go: -race requires cgo; enable cgo by setting CGO_ENABLED=1`

**Why:** The quality-gate container uses `golang:1.26-alpine` which lacks `gcc` and `musl-dev`. The `-race` flag requires CGO (a C compiler), so `go vet ./...` passes but `go test -race ./...` fails immediately.

**Failed step:** `go test -race ./...` (step 7 of 7 in the quality-gate script)
**All prior steps pass:** `apk add`, `git clone`, `go version`, `go vet ./...`

**Fix:** Add `gcc musl-dev` to the `apk --no-cache add` line in the quality-gate step of the `domain-check-build` WorkflowTemplate in `declarative-config`.

Full analysis saved to `.beads/traces/bf-29a0/quality-gate-failure-analysis.md`.
