Done. Here's the summary:

**Bead `bf-iaeg` closed** — Quality-gate logs were already captured from a prior debug workflow (`domain-check-qg-capture-v4nsl`) and saved to `docs/notes/quality-gate-logs-2026-07-04.md`.

**Findings:**
- **Exit code:** 2
- **`go vet ./...`**: Passes ✅
- **`go test -race ./...`**: Fails with `go: -race requires cgo; enable cgo by setting CGO_ENABLED=1`
- **Root cause:** The `golang:1.26-alpine` image has CGO disabled by default; the `-race` flag requires CGO
- **Fix needed:** Add `CGO_ENABLED=1` + `build-base` apk package to the quality-gate container in the `domain-check-build` WorkflowTemplate, or switch to `golang:1.26` (Debian-based)
