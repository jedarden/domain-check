Done. Here's the summary:

**Quality-gate logs captured** from pod `domain-check-qg-capture-v4nsl-build-quality-gate-3069676470` on iad-ci.

- **`go vet ./...`** — passes successfully (all dependencies downloaded, no issues)
- **`go test -race ./...`** — fails with `go: -race requires cgo; enable cgo by setting CGO_ENABLED=1`

**Root cause:** The `golang:1.26-alpine` image has CGO disabled by default, and `-race` requires CGO. The fix belongs in the WorkflowTemplate `domain-check-build` in declarative-config.

Logs documented in `docs/notes/quality-gate-logs-2026-07-04.md`. Bead `bf-2nqc` closed.
