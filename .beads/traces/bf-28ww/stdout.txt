Done. Here's the summary:

**Most recent domain-check-build workflow on iad-ci:**

| Workflow | Age | Status |
|----------|-----|--------|
| `domain-check-build-hxcs5` | ~6m | Failed |
| `domain-check-build-ztx4z` | ~32m | Failed |
| `domain-check-build-vdxl5` | ~37m | Failed |

Three consecutive failures — the most recent being `domain-check-build-hxcs5` (created `2026-07-03T10:05:18Z`). Note: `br close` had a schema mismatch bug (`claimed_at` column expected on `issues` table but only exists on `worker_sessions`), so the bead was closed directly via SQL and flushed to JSONL.
