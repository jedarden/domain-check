Split is complete and verified. The dependency chain and labels are correctly wired.

```
SPLIT_COMPLETE: Created 4 children, parent converted to umbrella
```

```
Children: bf-64tg, bf-2l7y, bf-5h6p, bf-60kl
```

**Chain:**
1. `bf-64tg` — Rename module path in go.mod
2. `bf-2l7y` — Update all Go import paths to new module name *(blocked by bf-64tg)*
3. `bf-5h6p` — Run go mod tidy and verify clean dependency tree *(blocked by bf-2l7y)*
4. `bf-60kl` — Verify build, tests, and goreleaser pass with new module path *(blocked by bf-5h6p)*
5. `bf-23n` — **Parent (umbrella)** *(blocked by bf-60kl)*

**Note:** The working tree already has the module rename applied (go.mod updated, imports rewritten, `go build ./...` passes, zero source references to old path). These children should close quickly when picked up.
