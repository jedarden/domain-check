**Did NOT split — closed as already-done instead.**

The fix this bead describes is already in place in both the declarative-config repo and the on-cluster WorkflowTemplate:

- `build-quality-gate` (line 152): ✅ `apk --no-cache add git ca-certificates`
- `quality-gate` (line 188): ✅ `apk --no-cache add git ca-certificates`
- `goreleaser-release` (line 224): ✅ `apk --no-cache add git ca-certificates`

All three templates using `golang:1.26-alpine` already install git before `git clone`. The 7 failures were likely caused by other issues (e.g., the bead worker retrying while the fix was being applied, or cluster sync lag), not a missing code change. No split was warranted.

```
CLOSED: bf-48x6 — Fix already applied, bead obsolete
```
