Bead `bf-60w` is closed. Here's a summary of what was done:

**Added goreleaser container step to `domain-check-build` WorkflowTemplate** in `/home/coding/declarative-config`:

- **New `workspace` volume** (`emptyDir`) added alongside the existing `docker-config` volume
- **New `goreleaser` step** wired into the build pipeline after `docker-build` (line 40-41)
- **New `goreleaser` template** (lines 228-266) with:
  - `goreleaser/goreleaser:v2` image (pinned, no `:latest`)
  - `workingDir: /workspace`
  - `goreleaser release --clean` command
  - Workspace volume mounted at `/workspace`
  - GH_TOKEN and GITHUB_TOKEN env vars from the existing secret
  - Same resource limits as other templates (4 CPU / 8Gi)
- YAML validated as parseable
- Committed and pushed to declarative-config main
