Done. Here's what was accomplished:

1. **Captured raw logs** directly from the still-existing pod `domain-check-qg-capture-v4nsl-build-quality-gate-3069676470` on iad-ci
2. **Saved to `docs/notes/quality-gate-logs-2026-07-04.txt`** — 65 lines of complete, untruncated output including:
   - Alpine package installation
   - Git clone + Go version check
   - `go vet ./...` (passed — no issues)
   - `go test -race ./...` (failed — `go: -race requires cgo; enable cgo by setting CGO_ENABLED=1`)
   - Full dependency download log and Argo executor exit messages
3. **Committed and pushed** to main
4. **Bead `bf-1gqk` closed**
