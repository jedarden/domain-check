Bead `bf-30i6` is closed. Here's the verification summary:

## Workflow Verification — `domain-check-build-manual-zwzbk`

### Checklist Results

| # | Check | Result |
|---|-------|--------|
| 1 | `.status.phase` is Succeeded | **FAIL** — actual: `Failed` |
| 2 | No `goreleaser-release` node | **PASS** — not present in node tree |
| 3 | All nodes are Succeeded | **FAIL** — `build-quality-gate` failed before `resolve-version` and `docker-build` could run |
| 4 | Expected steps: `build-quality-gate`, `resolve-version`, `docker-build` | **PARTIAL** — only `build-quality-gate` ran |

### Node Tree

```
domain-check-build-manual-zwzbk (Steps) — Failed
  └─ [0] (StepGroup) — Failed
       └─ build-quality-gate (Pod) — Failed  ← exit code 127
```

### Root Cause

**`build-quality-gate`** failed with **exit code 127** (command not found). The step uses image `golang:1.26-alpine` and runs a script starting with `git clone`. Alpine-based golang images do **not** include `git` by default — the `git clone` command can't be found, causing exit code 127.

### Fix Needed

Add `apk add --no-cache git` before the `git clone` step in the `build-quality-gate` container args, or switch from `golang:1.26-alpine` to `golang:1.26` (Debian-based, which includes git).
