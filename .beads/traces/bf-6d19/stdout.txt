All done. Here's the summary:

**Bead bf-6d19 — Verified: all acceptance criteria met.**

`docs/quality-gate-logs.md` contains the complete, untruncated container output from debug workflow `domain-check-build-debug-podgc2-5dxln`. All 5 steps are present:

| # | Step | Line |
|---|------|------|
| 1 | `apk --no-cache add git ca-certificates` | 12 |
| 2 | `git clone --branch main` | 28 |
| 3 | `go version` → `go1.26.4 linux/amd64` | 31–32 |
| 4 | `go vet ./...` | 34 |
| 5 | `go test -race ./...` → **FAILED** | 59–60 |

The root-cause error `go: -race requires cgo; enable cgo by setting CGO_ENABLED=1` appears on line 60. No steps or output are missing.
